# =================================
# DWC SERVER - ENVIRONMENT CONFIGURATION
# =================================
# Copy this file to .env and configure your settings
#
# IMPORTANT: Choose the right configuration for your setup:
# - docker-compose.ghcr.yml         → SQLite (quick setup)
# - docker-compose.ghcr-mariadb.yml → MariaDB + GTS (full features)
# - docker-compose.dev.yml          → Local development builds

# =================================
# DJANGO SETTINGS (Required)
# =================================
# Generate with: openssl rand -base64 50
SECRET_KEY=change-me-in-production

# Debug mode (set to False in production)
DEBUG=False

# Allowed hosts (comma-separated, use your domain in production)
ALLOWED_HOSTS=localhost,127.0.0.1,*

# =================================
# API AUTHENTICATION (Required)
# =================================
# Generate with: openssl rand -hex 20
NAS_API_TOKEN=change-me-generate-a-token

# API URL (internal Docker network)
API_BASE_URL=http://admin:7999/api

# =================================
# DATABASE CONFIGURATION
# =================================
# For docker-compose.ghcr.yml (SQLite):
DATABASE_ENGINE=sqlite
DATABASE_NAME=/app/data/dwc_server.db

# For docker-compose.ghcr-mariadb.yml (MariaDB + GTS):
# DATABASE_ENGINE=mariadb
# DATABASE_NAME=dwc_server
# DATABASE_USER=dwc
# DATABASE_PASSWORD=changeme
# DATABASE_HOST=mariadb
# DATABASE_PORT=3306
# DATABASE_ROOT_PASSWORD=rootpassword

# =================================
# GTS (GLOBAL TRADE STATION)
# =================================
# Only required for docker-compose.ghcr-mariadb.yml
# Pokemon GTS database credentials
GTS_DB_NAME=gts
GTS_DB_USER=gts
GTS_DB_PASSWORD=gts

# =================================
# ADMIN PANEL (Optional)
# =================================
# Superuser auto-creation (set all three to enable)
# Uncomment and set to create a Django admin user on first startup:
# DJANGO_SUPERUSER_USERNAME=admin
# DJANGO_SUPERUSER_PASSWORD=changeme
# DJANGO_SUPERUSER_EMAIL=admin@example.com

# Gunicorn settings (performance tuning)
GUNICORN_WORKERS=2
GUNICORN_TIMEOUT=120

# =================================
# LOGGING
# =================================
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# =================================
# CONFIGURATION EXAMPLES
# =================================

# Example 1: Quick Production Setup (SQLite)
# ------------------------------------------
# Use with: docker-compose.ghcr.yml
#
# SECRET_KEY=your-generated-secret-key-here
# NAS_API_TOKEN=your-generated-api-token-here
# DATABASE_ENGINE=sqlite
# DATABASE_NAME=/app/data/dwc_server.db
# DEBUG=False

# Example 2: Full Production Setup (MariaDB + GTS)
# -------------------------------------------------
# Use with: docker-compose.ghcr-mariadb.yml
#
# SECRET_KEY=your-generated-secret-key-here
# NAS_API_TOKEN=your-generated-api-token-here
# DATABASE_ENGINE=mariadb
# DATABASE_NAME=dwc_server
# DATABASE_USER=dwc
# DATABASE_PASSWORD=your-secure-password
# DATABASE_HOST=mariadb
# DATABASE_PORT=3306
# DATABASE_ROOT_PASSWORD=your-root-password
# GTS_DB_NAME=gts
# GTS_DB_USER=gts
# GTS_DB_PASSWORD=your-gts-password
# DEBUG=False

# Example 3: Development Setup
# -----------------------------
# Use with: docker-compose.dev.yml
#
# SECRET_KEY=dev-secret-key-not-for-production
# NAS_API_TOKEN=dev-token
# DATABASE_ENGINE=sqlite
# DATABASE_NAME=/app/data/dwc_server.db
# DEBUG=True
# LOG_LEVEL=DEBUG
